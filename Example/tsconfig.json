{
  "extends": "../tsconfig",
  "references": [
    { "path": "../packages/image-transformer" },
    { "path": "../packages/pager" },
  ],
  "compilerOptions": {
    // Avoid expo-cli auto-generating a tsconfig
  }
}
